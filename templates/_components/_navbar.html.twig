<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <img src="{{ asset('images/logo/logo.png') }}" class="logo" alt="logo">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item">
                  <a class="nav-link" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
              </li>
              <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Préparer sa venue
                  </a>
              <ul class="dropdown-menu">
                  <li><a class="dropdown-item" href="#">Voir les horaires</a></li>
                  <li><a class="dropdown-item" href="#">Où nous trouver</a></li>
                  <li><a class="dropdown-item" href="#">Les tarifs</a></li>
              </ul>
              </li>
              <li class="nav-item">
                  <a class="nav-link" href="{{ path('app_service') }}">Les services</a>
              </li>
               <li class="nav-item">
                  <a class="nav-link" href="{{ path('app_habitat') }}">Les habitats</a>
               </li>
               <li class="nav-item">
                  <a class="nav-link" href="#">Contact</a>
               </li>
               {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link yellow" href="{{ path('admin')}}">Espace gestion</a>
                    </li>
               {% endif %}
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link logout-button" href="{{ path('app_logout')}}">Déconnexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link login-button" href="{{ path('app_login')}}">Connexion</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>